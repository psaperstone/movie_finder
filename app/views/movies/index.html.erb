<div class="my-movies">
<p id="notice"><%= notice %></p>

<h1 class"my-movies">My Movies</h1>

<table class="my-movies">
  <thead>
    <tr>
      <th>Title</th>
      <th>Year</th>
      <th>Genre</th>
      <!-- <th>Metascore</th>
      <th>Imdbscore</th> -->
      <th>User rating</th>
      <!-- <th colspan="3"></th> -->
    </tr>
  </thead>

  <tbody>
    <% @movies.each do |movie| %>
      <tr>
        <td><%= movie.title %></td>
        <td><%= movie.year %></td>
        <td><%= movie.genre %></td>
        <!-- <td><%#= movie.actors %></td>
        <td><%#= movie.metascore %></td>
        <td><%#= movie.imdbscore %></td> -->
        <td><%= movie.user_rating %></td>
        <td><%= link_to 'Show', movie, class: 'btn btn-default btn-xs' %></td>
        <td><%= link_to 'Edit', edit_movie_path(movie), class: 'btn btn-default btn-xs' %></td>
        <td><%= link_to 'Destroy', movie, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-default btn-xs' %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Home', current_user, class: 'btn btn-default' %>
<%= link_to new_movie_path, class: 'btn btn-default' do %>
  <i class="glyphicon glyphicon-plus"></i> New Movie
  <% end %>
</div>
